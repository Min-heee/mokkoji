/**
 * 엔빵 디자인 토큰 — 다크 차콜 바이올렛 + 네온 그린옐로 포인트.
 * 모던·심플: 배경은 짙은 바이올렛, 강조/CTA는 네온 한 가지로만.
 */
export const colors = {
  /** 앱 배경 — 아주 짙은 차콜 바이올렛 */
  bg: '#1A0E22',
  /** 카드/표면 */
  card: '#2A1435',
  /** 입력·칩 등 살짝 밝은 표면 (레퍼런스 Charcoal Violet 계열) */
  cardAlt: '#3C1A47',
  /** 본문 텍스트 — 웜 화이트 */
  text: '#F4EEF8',
  /** 보조 텍스트 — 뮤트 라벤더 */
  subtext: '#A88CB8',
  /** 경계선 */
  border: '#402350',
  /** 포인트·CTA — 네온 그린옐로 */
  primary: '#B6FF00',
  /** 네온 위에 얹는 텍스트 — 근접 블랙 */
  onPrimary: '#16130A',
  /** 네온의 은은한 배경 버전 (다크 위 ghost/선택) */
  primaryDim: 'rgba(182,255,0,0.14)',
  /** 받을 돈 등 긍정 — 민트 (네온과 구분) */
  success: '#5DE0A6',
  /** 보낼 돈·삭제 등 — 코랄 */
  danger: '#FF5C7A',
  /** 코랄의 은은한 배경 버전 */
  dangerDim: 'rgba(255,92,122,0.15)',
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  pill: 999,
};

export const fontSize = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 20,
  xl: 26,
};
